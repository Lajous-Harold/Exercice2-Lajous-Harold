# --- API ---
PORT=3000

# --- Mongo ---
MONGODB_URI=mongodb://mongo:27017/todolist

# --- Postgres ---
POSTGRES_URL=postgresql://app:app@postgres:5432/todolist
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=todolist

# --- JWT ---
JWT_SECRET=change-me-super-secret
TOKEN_TTL=1h

# --- Demo user pour /auth/login ---
AUTH_USER=admin
# Hash généré avec: node -e "console.log(require('bcryptjs').hashSync('motdepasse', 10))"
AUTH_PASS_HASH=$2a$10$replace_me_with_a_real_hash
